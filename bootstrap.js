console.log('bootstrap.js LOADED');

if ('querySelector' in document) {
  // If the browser has native CSS selecting then it removes the need for a DOM selector.
}

if ('localStorage' in window) {
}

if ('addEventListener' in window) {
  // If the browser supports this then we know it has better support than IE8.
}